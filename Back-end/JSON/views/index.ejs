<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->
   
    
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
     <% if(locals.select){%>
      <h2>Selected Recipe: <%= select.name %></h2>
      <p>Price: $<%= select.price %></p>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>Protein: <%= select.ingredients.protein.name %> - <%= select.ingredients.protein.preparation %></li>
        <li>Salsa: <%= select.ingredients.salsa.name %></li>
        <% select.ingredients.toppings.forEach(function(topping) { %>
          <li><%= topping.name %> - <%= topping.quantity %></li>
        <% }) %>
      </ul>
    </ul>
  
    <% }else { %>
    <h2>Pick your favourite taco ingredient 👆</h2> 
    <% } %>




  </div>
</body>

</html>